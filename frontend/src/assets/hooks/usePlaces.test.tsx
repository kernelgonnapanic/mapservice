import React from 'react'

describe('usePlaces', () => {
	xit('return placesLoading = true when data is being fetched', () => {
		throw new Error('Not implemented')
	})

	xit('return hasMoreData = true when there is more data to be fetched', () => {
		throw new Error('Not implemented')
	})

	xit('when page number changes loads more data', () => {
		throw new Error('Not implemented')
	})
})
